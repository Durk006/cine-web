//get the watch history and show the top 20

import UploadForm from '../components/api/UploadForm';

export default function watchHistory (){
  return (
    <div>
      <h1>Upload CSV to Cloudinary</h1>
      <UploadForm />
    </div>
  );
};
